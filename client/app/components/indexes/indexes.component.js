import template from './indexes.html';
import controller from '../home/home.controller';
import './indexes.scss';

let indexesComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default indexesComponent;
